<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/eng_background"> <!-- Ripple Effect 색상 -->

    <!-- 배경색 -->
    <item android:id="@+id/ripple_eng_box"
        android:top="-4dp"
        android:left="-4dp"
        android:right="-4dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/main_white"/>
            <stroke android:width="3dp" android:color="@color/eng_background"/>
        </shape>
    </item>

</ripple>