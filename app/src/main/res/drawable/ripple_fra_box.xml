<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/main_white"> <!-- Ripple Effect 색상 -->

    <!-- 배경색 -->
    <item android:id="@+id/ripple_fra_box">
        <shape android:shape="rectangle">
            <gradient android:startColor="@color/fra_background"
                android:endColor="@color/fra_light"/>
            <stroke android:width="2dp" android:color="@color/fra_light"/>
        </shape>
    </item>

</ripple>