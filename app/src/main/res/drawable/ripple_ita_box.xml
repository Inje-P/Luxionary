<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/main_white"> <!-- Ripple Effect 색상 -->

    <!-- 배경색 -->
    <item android:id="@+id/ripple_ita_box">
        <shape android:shape="rectangle">
            <gradient android:startColor="@color/ita_background"
                android:endColor="@color/ita_light"/>
            <stroke android:width="2dp" android:color="@color/ita_light"/>
        </shape>
    </item>

</ripple>