<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Luxionary" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_text</item>
        <item name="colorPrimaryVariant">@color/main_text</item>
        <item name="colorOnPrimary">@color/main_text</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_text</item>
        <item name="colorSecondaryVariant">@color/main_text</item>
        <item name="colorOnSecondary">@color/main_text</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="DialogStyle" parent="Theme.AppCompat.Dialog">
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:windowMinWidthMinor">80%</item>
    </style>

    <style name="DialogStyle2" parent="Theme.AppCompat.Dialog">
        <item name="android:windowMinWidthMajor">100%</item>
        <item name="android:windowMinWidthMinor">100%</item>
    </style>

    <style name="toastEng">
        <item name="stTextBold">true</item>
        <item name="stTextColor">@color/main_white</item>
        <item name="stTextSize">16dp</item>
        <item name="stColorBackground">@color/eng_background</item>
        <item name="stStrokeWidth">0dp</item>
        <item name="stIconStart">@drawable/ic_toast</item>
        <item name="stLength">SHORT</item>
    </style>

    <style name="toastFra">
        <item name="stTextBold">true</item>
        <item name="stTextColor">@color/main_white</item>
        <item name="stTextSize">16dp</item>
        <item name="stColorBackground">@color/fra_background</item>
        <item name="stStrokeWidth">0dp</item>
        <item name="stIconStart">@drawable/ic_toast</item>
        <item name="stLength">SHORT</item>
    </style>

    <style name="toastDeu">
        <item name="stTextBold">true</item>
        <item name="stTextColor">@color/main_white</item>
        <item name="stTextSize">16dp</item>
        <item name="stColorBackground">@color/deu_background</item>
        <item name="stStrokeWidth">0dp</item>
        <item name="stIconStart">@drawable/ic_toast</item>
        <item name="stLength">SHORT</item>
    </style>

    <style name="toastIta">
        <item name="stTextBold">true</item>
        <item name="stTextColor">@color/main_white</item>
        <item name="stTextSize">16dp</item>
        <item name="stColorBackground">@color/ita_background</item>
        <item name="stStrokeWidth">0dp</item>
        <item name="stIconStart">@drawable/ic_toast</item>
        <item name="stLength">SHORT</item>
    </style>

    <style name="toastSpa">
        <item name="stTextBold">true</item>
        <item name="stTextColor">@color/main_white</item>
        <item name="stTextSize">16dp</item>
        <item name="stColorBackground">@color/spa_background</item>
        <item name="stStrokeWidth">0dp</item>
        <item name="stIconStart">@drawable/ic_toast</item>
        <item name="stLength">SHORT</item>
    </style>

    <style name="toastRus">
        <item name="stTextBold">true</item>
        <item name="stTextColor">@color/main_white</item>
        <item name="stTextSize">16dp</item>
        <item name="stColorBackground">@color/rus_background</item>
        <item name="stStrokeWidth">0dp</item>
        <item name="stIconStart">@drawable/ic_toast</item>
        <item name="stLength">SHORT</item>
    </style>

</resources>